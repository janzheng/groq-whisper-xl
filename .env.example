# Environment variables for Groq Whisper XL CLI
# Copy this file to .env and add your actual values

# Production API endpoint (your deployed Cloudflare Worker URL)
PRODUCTION_URL=https://your-worker-name.your-subdomain.workers.dev

# Local development endpoint (usually localhost during development)
LOCAL_URL=http://localhost:8787
